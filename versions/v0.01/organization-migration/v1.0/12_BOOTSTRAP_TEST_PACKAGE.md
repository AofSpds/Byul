# Bootstrap / Fresh-Channel Test Package

STATE = TEST_SPEC_READY / FRESH_CHANNEL_EXECUTION_NOT_PERFORMED
SELF_CHECK_IS_NOT_VALIDATION_PASS = TRUE

Required tests:
- T1 BYUL: selector -> common + BYUL memory -> current task/blocker; no AAA/ASA-MI runtime dependency.
- T2 PMO: selector -> PMO execution status/WP registry; no Owner relay required.
- T3 CONTROL: current refs/authority/registry recovered; no invented state.
- T4 MODEL: current model hypotheses/open questions recovered; no implementation authority inflation.
- T5 ENG: authorized implementation scope/tests recovered; no semantic design mutation.
- T6 Validator: exact target/criteria/source refs load without author persuasive memory.
- T7 IVA: audit-required package + exact evidence only; organization-external posture preserved.
- T8 Conflict injection: memory says A, registry/governed state says B -> governed state wins and REVIEW_REQUIRED.

Static authoring invariants to be checked before fresh-channel execution:
1. selector keys unique case-insensitively;
2. each selector maps to exactly one PERSONA typed object;
3. Project BYUL object ID differs from Persona BYUL object ID;
4. paired relationships symmetric; IVA has no pair;
5. no RES selector or memory route exists initially;
6. memory index covers all 11 initial Personas;
7. all candidate paths stay under migration branch namespace;
8. WP9 remains unavailable.

Pass state can only be issued after actual isolated fresh-channel runs and paired validation receipts.