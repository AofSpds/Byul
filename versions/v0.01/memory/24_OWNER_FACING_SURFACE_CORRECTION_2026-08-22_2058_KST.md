# 24. BYUL Owner-Facing Surface Correction — 2026-08-22 20:58 KST

STATUS = OWNER_DIRECTION / ORCHESTRATION_SURFACE_CORRECTION / NON_NORMATIVE
PROJECT = BYUL
CURRENT_MIGRATION_PERSONA = ASA-MI
AAA_MUTATION_AUTHORIZED = FALSE

## Owner Correction

1. `ASA-MI` is the temporary planning / architecture / design surface and is operated in a ChatGPT Channel Pro-style reasoning surface.
2. `ASA-ME` is the temporary execution PMO surface and is operated in ChatGPT WORK Ultra for program execution / orchestration.
3. The prior proposal that reduced the Owner-facing surface to one PMO Main Control Channel is rejected for BYUL.
4. The post-migration BYUL organization must preserve two distinct primary Owner-facing functions:
   - a planning / architecture / design Persona surface (working label: `BYUL`, final name open);
   - an execution / PMO Persona surface.
5. Each of those primary surfaces has its own paired Validator surface.
6. An organization-external Independent Validator/Auditor surface also exists separately.
7. The Project Owner directly faces the planning/design surface, execution/PMO surface, their paired Validators, and the organization-external Independent Validator/Auditor.
8. Owner-facing plurality is intentional; Owner is still not a manual relay for worker-thread results. Each primary surface remains responsible for its own orchestration/consolidation discipline.
9. Default execution under the execution/PMO surface may still use Persona-injected Agent Threads and Git-persisted work packets/journals/checkpoints.
10. No AAA code, organization, Persona registry, authority, memory structure, Shared Contract, release, or production state may be modified.

## Naming Review

The final name for the planning/design Owner-facing Persona is OPEN.

Using `BYUL` itself is possible but may conflate project identity with Persona identity. The migration review should explicitly compare:
- `BYUL` as both project name and planning/design Persona;
- a qualified functional name derived from BYUL;
- a separate proper Persona name with explicit `PROJECT=BYUL` binding.

No name is selected by this checkpoint.

## Item-8 Consequence

Item 8 must be rewritten from:

`OWNER -> PMO MAIN as the single default Owner-facing surface`

to:

`OWNER directly faces the planning/design primary surface, execution/PMO primary surface, each paired Validator, and an organization-external Independent Validator/Auditor; worker execution remains delegated beneath those surfaces.`
